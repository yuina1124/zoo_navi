animal#show

<%= @animal.each do |zoo| %>
  <% if animal.image.attached? %>
    <%= image_tag zoo.image, size: "200x200" %>
  <% else %>
    <%= image_tag 'noimage', size: "200x200" %>
  <% end %>
<% end %>